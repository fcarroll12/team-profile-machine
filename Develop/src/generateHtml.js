const renderList = (list) => {
    return list.items
      .map((item) => `<li class="list-group-item">${item.toString()}</li>`)
      .join("");
  };
  
  const generateList = (list) => {
      return `
      
      `;

  };